import HardwareDeck from "../components/store/HardwareDeck";

/** Fleet — business AI computers and servers. Old 30-AI runway lives at /fleet-runway. */
export default function Fleet() {
  return <HardwareDeck mission="business" />;
}
